targets:
  aws: prod-aws
scenarios:
  - name: targeted aws
    detonate:
      awsCliDetonator:
        script: "true"
    expectations:
      - timeout: 1m
        datadogSecuritySignal:
          name: "Test signal"
